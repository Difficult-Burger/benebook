<!DOCTYPE html>
<html lang="zh">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - Benebook</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
  <div class="container py-5">
    <nav aria-label="breadcrumb" class="mb-4">
      <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="/" class="text-decoration-none">首页</a></li>
        <li class="breadcrumb-item active" aria-current="page"><%= book.title %></li>
      </ol>
    </nav>

    <div class="row g-4">
      <div class="col-md-4 mb-4">
        <div class="card">
          <div class="bg-light text-center p-3" style="height: 300px; display: flex; align-items: center; justify-content: center;">
            <% if (book.image_url) { %>
              <img src="<%= book.image_url %>" alt="<%= book.title %>" style="max-height: 280px; max-width: 100%;">
            <% } else { %>
              <span class="text-muted">暂无图片</span>
            <% } %>
          </div>
        </div>
      </div>
      <div class="col-md-8">
        <h1 class="mb-3"><%= book.title %></h1>
        <div class="mb-4">
          <span class="badge bg-primary me-2"><%= book.category || '未分类' %></span>
          <span class="badge bg-secondary">状态: <%= Math.round(book.condition_ratio * 100) %>%</span>
        </div>

        <table class="table table-striped">
          <tbody>
            <tr>
              <th style="width: 120px;">作者</th>
              <td><%= book.author || '未知' %></td>
            </tr>
            <tr>
              <th>出版社</th>
              <td><%= book.publisher || '未知' %></td>
            </tr>
            <tr>
              <th>位置</th>
              <td><%= book.location || '未指定' %></td>
            </tr>
            <tr>
              <th>原价</th>
              <td class="fw-bold">¥<%= book.original_price %></td>
            </tr>
          </tbody>
        </table>

        <a href="/" class="btn btn-primary mt-3">返回列表</a>
      </div>
    </div>
  </div>
</body>
</html>