<!DOCTYPE html>
<html lang="zh">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
  <div class="container py-5">
    <nav aria-label="breadcrumb" class="mb-4">
      <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="/" class="text-decoration-none">首页</a></li>
        <li class="breadcrumb-item active" aria-current="page">捐赠者列表</li>
      </ol>
    </nav>

    <div class="d-flex justify-content-between align-items-center mb-4">
      <h1 class="h2">捐赠者列表</h1>
      <a href="/donors/new" class="btn btn-primary">添加新捐赠者</a>
    </div>

    <% if (donors && donors.length > 0) { %>
      <div class="table-responsive">
        <table class="table table-striped table-hover">
          <thead>
            <tr>
              <th>手机号</th>
              <th>昵称</th>
              <th>邮箱</th>
              <th>微信号</th>
              <th>积分</th>
              <th>操作</th>
            </tr>
          </thead>
          <tbody>
            <% donors.forEach(donor => { %>
              <tr>
                <td><%= donor.donor_phone %></td>
                <td><%= donor.nickname %></td>
                <td><%= donor.email || '-' %></td>
                <td><%= donor.wechat_id || '-' %></td>
                <td><%= donor.total_points %></td>
                <td>
                  <a href="/donors/<%= donor.donor_phone %>" class="btn btn-sm btn-info">查看</a>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    <% } else { %>
      <div class="alert alert-info">
        暂无捐赠者数据，请添加捐赠者。
      </div>
    <% } %>
    
    <div class="mt-4">
      <a href="/" class="btn btn-outline-secondary">返回首页</a>
    </div>
  </div>
</body>
</html>